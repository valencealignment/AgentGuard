[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "waasl-research-engine"
version = "0.1.0"
description = "Research helpers and generated attack fixtures for WAASL MERCK."
requires-python = ">=3.12"
