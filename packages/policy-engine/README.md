# WAASL Policy Engine

This package exposes the rule-loading and corpus-evaluation helpers used by the
MERCK loop. It intentionally wraps the repo-local runtime logic so the package
stays consistent with the active firewall implementation.
