version: 1
rules:
  package_blocklist:
    - name: "litellm"
      versions: ["1.82.8"]
      reason: "Supply chain attack"
      severity: critical

  reputation_thresholds:
    block_below_score: 10
    warn_below_score: 30

  typosquatting:
    enabled: false

  behavioral_signals:
    block_if: []
    warn_if: []

  mcp_rules:
    block_if: []
    warn_if: []

  domain_blocklist: []
