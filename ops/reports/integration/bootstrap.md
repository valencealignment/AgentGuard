# Integration Bootstrap

Shared integration substrate initialized by the `codex/mail-core` lane.

Completed:

- repo constitution in `AGENTS.md`
- shared contracts in `packages/contracts`
- repo-native coordination helpers in `scripts/ops`
- repo-local hook config in `.codex/hooks.json`
- aggregate watchboard state generation
- minimal API in `apps/api`
- Modal-compatible summarizer scaffold in `infra/modal`

Next:

- keep aggregate state fresh as other lanes land
- integrate lane branches conservatively
- maintain API compatibility with repo-artifact fallbacks
